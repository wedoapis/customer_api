require "rails_helper"

RSpec.describe CustomersController do
  describe "GET index" do
    it "responds with customers" do
      # team = Customer.create
      # get :index
      # expect(response.body).to eq([])
    end
  end
end
